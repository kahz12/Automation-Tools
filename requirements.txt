# Dependencies live in pyproject.toml — this file just points at them so the
# familiar `pip install -r requirements.txt` keeps working. Run it from the
# project root.
#
# Runtime only:  pip install -e .
# With tests:    pip install -e ".[dev]"

-e .[dev]
